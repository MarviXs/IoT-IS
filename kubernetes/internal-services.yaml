apiVersion: v1
kind: Service
metadata:
  name: pg
spec:
  type: ClusterIP  # Internal service
  ports:
    - port: 5432
  selector:
    app: pg
---
apiVersion: v1
kind: Service
metadata:
  name: timescaledb
spec:
  type: ClusterIP  # Internal service
  ports:
    - port: 5432
  selector:
    app: timescaledb
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis